# The API key is scoped to one city and is required on every request as an
# X-API-Key header. It is not a secret in any meaningful sense here - the app is
# client side, so it ships in the bundle either way - but keeping it in an env
# var makes it swappable without a code change.
VITE_IVY_API_KEY=IVY26-XXXXXXXXXXXX
